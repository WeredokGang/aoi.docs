# $serverIcon
desc
### Использование
```php
$serverIcon[сервер?]
```

### Опции

| Опция | Описание | Тип | Обязательно |
|--------|-------------|------|----------|
| сервер |  |  | Нет |  
## Пример(ы)

```javascript
bot.command({
  name: '$serverIcon',
  code: `
$serverIcon[сервер?]`
// Возвращает: ...
})
```
