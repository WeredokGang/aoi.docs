# $dbPing
Возвращает пинг получения данных из базы данных переменных
### Использование
```php
$dbPing
```

## Пример(ы)

```javascript
bot.command({
  name: '$dbPing',
  code: `
$dbPing`
// Возвращает: 0
})
```
