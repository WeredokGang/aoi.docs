# $clientToken
Возвращает токен клиента
### Использование
```php
$clientToken
```

## Пример(ы)

```javascript
bot.command({
  name: '$clientToken',
  code: `
$clientToken`
// Возвращает: Вы думали тут будет токен клиента моего бота? 
})
```
