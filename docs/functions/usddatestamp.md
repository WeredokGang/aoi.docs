# $datestamp
Возвращает текущее время в миллисекундах
### Использование
```php
$datestamp
```

## Пример(ы)

```javascript
bot.command({
  name: '$datestamp',
  code: `
$datestamp`
// Возвращает: 16467939761
})
```
