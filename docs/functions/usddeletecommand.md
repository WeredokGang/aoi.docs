# $deleteCommand
Удаляет сообщение, которое вызвало функцию
### Использование
```php
$deleteCommand
```

## Пример(ы)

```javascript
bot.command({
  name: '$deleteCommand',
  code: `
$deleteCommand`
})
```
